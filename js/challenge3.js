window.addEventListener('load', function() {
  // this.alert(document.querySelectorAll('figcaption')[1].innerHTML);

  let figcaptions = document.querySelectorAll('figcaption');
  let secondFigcaptio = figcaptions[1];
  this.alert(secondFigcaptio.innerHTML);
  // let text = secondFigcaptio.innerHTML;
  // let text = secondFigcaptio.textContent;

  // secondFigcaptio.text
});